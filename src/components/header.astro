<header class='w-full h-14 relative'>
	<div class='text-white flex justify-evenly mb-60 fixed items-center w-full bg-neutral-900 z-10 py-3'>
		<a href='/' class='dg'>
			<h2 class='text-2xl diego'>
				<span class='mr-1 text-rose-400 font-bold'>D</span> G
			</h2>
		</a>
		<ol class='flex gap-10 text-xl'>
			<li><a class='descrip relative' href='/'>Home</a></li>
			<li class='dg'><a class='descrip relative dg' href='/blog'>blog</a></li>
			<li><a class='descrip relative' href='/about'>About</a></li>
			<li><a class='descrip relative' href='/projects'>Projects</a></li>
			<li class='dg'><a class='descrip relative dg' href='#contacto'>Contacto</a></li>
		</ol>
	</div>
</header>

<style>
	@media (max-width: 700px) {
		.dg {
			display: none;
		}
		.second_container {
			width: 100%;
		}

		.frase {
			display: none;
		}
	}
	.frase {
		background-size: cover;
		-moz-background-clip: text;
		-webkit-background-clip: text;
		text-transform: uppercase;
	}
	.frase {
		overflow: hidden;
		white-space: nowrap;
		border-right: 0.15em solid #274a2e;
		animation: typing 6s steps(30) infinite, blink 2s steps(30) infinite;
	}
	@keyframes typing {
		0% {
			width: 0;
		}
		50% {
			width: calc(100% + 10px);
		}
		70% {
			width: calc(100% + 10px);
		}
		100% {
			width: 0;
		}
	}
	@keyframes blink {
		50% {
			border-color: transparent;
		}
	}
	.name {
		text-shadow: 0 1.5px 0.5px black;
	}

	.descrip::after {
		content: '';
		width: 100%;
		background-color: #ea7987;
		position: absolute;
		left: 0;
		bottom: 0;
		transform: scaleX(0);
		transform-origin: left;
		transition: transform 0.3s ease-in-out;
	}
	.descrip:hover::after {
		height: 2px;
		transform: scaleX(1);
	}
	.diego:hover {
		transform: scale(1.2);
	}
	.diego {
		transition: transform ease 0.3s;
	}
</style>
