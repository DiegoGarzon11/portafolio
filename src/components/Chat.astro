<script src='./ejemplo.js'></script>

<div class='flex items-center justify-center'>
	<div class='flex flex-col h-full bg-gray-100 w-4/5 rounded-lg'>
		<div class='flex-1 px-4 py-8 overflow-y-auto'>
			<div class='flex flex-col space-y-4'>
				<div class='flex items-center justify-end'>
					<div class='bg-blue-500 text-white rounded-lg px-4 py-2 max-w-xs'>
						<p id='text-user'>:)</p>
					</div>
				</div>
				<div class='flex items-center justify-start'>
					<div class='bg-gray-200 text-gray-700 rounded-lg px-4 py-2 max-w-xs'>
						<p id='text-chat' class='textChat'>
							Por favor dame tu nombre para poder entablar una mejor conversacion, mi
							nombre es ChatGo. :)
						</p>
					</div>
				</div>
			</div>
		</div>
		<div class='px-4 py-2'>
			<form>
				<div class='flex rounded-lg border-gray-400 border-2'>
					<input
						type='text'
						id='input-chat'
						class='flex-1 rounded-l-lg p-2 focus:outline-none'
						placeholder='Type a message...'
					/>
					<button
						class='bg-blue-500 text-white rounded-r-lg px-4 py-2 hover:bg-blue-600 transition-colors'
						disabled
						id='btn-enviar'
						type='submit'>
						Send
					</button>
				</div>
			</form>
		</div>
	</div>
</div>
<!-- <style>
	.textChat {
		overflow: hidden;
		height: auto;
		width: auto;
		white-space: nowrap;
		border-right: 0.15em solid #274a2e;
		animation: typing 6s steps(30) infinite, blink 2s steps(30) infinite;
	}
	@keyframes typing {
		0% {
			width: 0;
		}
		50% {
			width: calc(100% + 10px);
		}
		70% {
			width: calc(100% + 10px);
		}
	}
	@keyframes blink {
		50% {
			border-color: transparent;
		}
	}
</style> -->
